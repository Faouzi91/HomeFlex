# Core

Application-wide technical foundations live here.

- `api/`
  HTTP clients and API-facing tests.
- `models/`
  Shared domain contracts returned by the backend.
- `state/`
  Global client state such as authentication/session.
- `utils/`
  Pure formatting and helper functions.
