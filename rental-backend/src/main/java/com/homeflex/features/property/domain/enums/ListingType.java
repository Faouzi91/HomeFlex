package com.homeflex.features.property.domain.enums;

public enum ListingType {
    RENT,
    SALE,
    SHORT_TERM,
    /** Per-night hotel/hostel rate — distinct from vacation-rental SHORT_TERM */
    NIGHTLY
}
